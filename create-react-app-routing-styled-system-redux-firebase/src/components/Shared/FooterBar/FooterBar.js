import { Footer, FooterWrapper } from './FooterBar.styled'

const FooterBar = () => {
  return (
    <Footer className="footer-bar">
      <FooterWrapper className="container">
        <div style={{ marginLeft: 'auto', marginRight: 'auto' }}>FooterBar</div>
      </FooterWrapper>
    </Footer>
  )
}

export default FooterBar
